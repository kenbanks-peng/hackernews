[tasks.build-production]
description = "Build the production binary"
run = "cargo build --release"

[tasks.deploy]
description = "Build and install hackernews_tui to ./bin"
depends = ["build-production"]
run = "mkdir -p ./bin && cp target/release/hackernews_tui ./bin/hackernews_tui"
